{
  "ignore": [
    "**/*.test.ts",
    "**/*.spec.ts",
    ".git",
    "node_modules"
  ],
  "watch": [
    "src/**/*.ts",
    "src/**/*.env"
  ],
  "exec": "npm run build:only && npm start",
  "ext": "ts"
}
